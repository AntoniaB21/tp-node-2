const sortedArray = require('./sortedArray.js');
const tri = require('./slug.js');
const upperCase = require('./upperCase.js');
const randomElement = require('./random-element.js');

module.exports = { sortedArray, tri, upperCase, randomElement};